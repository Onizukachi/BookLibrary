<!DOCTYPE html>
<html lang="en">
  <head>
    <title>BookLibrary</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <main class="flex">
      <nav class="flex">
        <div class="w-64 p-4 h-screen bg-gray-600 sticky top-0">
          <div class="flex items-center">
            <%= image_tag "logo.png", class: "w-16" %>
            <h1 class="ml-2 text-md py-2 text-gray-50 font-bold hidden md:block">
              BookLib Admin
            </h1>
          </div>
          <ul class="flex mt-4 flex-col text-gray-50">
            <li>
              <%= link_to admin_path, class: "flex gap-2 px-4 py-2 hover:rounded hover:bg-gray-700" do %>
                <%= icon "home", class: "w-6" %>
                <span class="hidden md:inline-block">Dashboard</span>
              <% end %>
            </li>
            <li>
              <%= link_to admin_products_path, class: "flex gap-2 px-4 py-2 hover:rounded hover:bg-gray-700" do %>
                <%= icon "shopping-cart", class: "w-6" %>
                <span class="hidden md:inline-block">Products</span>
              <% end %>
            </li>
            <li>
              <%= link_to admin_categories_path, class: "flex gap-2 px-4 py-2 hover:rounded hover:bg-gray-700" do %>
                <%= icon "queue-list", class: "w-6" %>
                <span class="hidden md:inline-block">Categories</span>
              <% end %>
            </li>
            <li>
              <%= link_to admin_orders_path, class: "flex gap-2 px-4 py-2 hover:rounded hover:bg-gray-700" do %>
                <%= icon "truck", class: "w-6" %>
                <span class="hidden md:inline-block">Orders</span>
              <% end %>
            </li>
            <li>
              <%= link_to destroy_admin_session_path, data: { turbo_method: :delete, turbo_confirm:  "Are you sure?" }, class: "flex gap-2 px-4 py-2 hover:rounded hover:bg-gray-700" do %>
                <%= icon "arrow-right-start-on-rectangle", class: "w-6" %>
                <span class="hidden md:inline-block">Sign Out</span>
              <% end %>
            </li>
          </ul>
        </div>
      </nav>
      <div class="p-8 w-full">
        <%= yield %>
      </div>
    </main>
  </body>
</html>
